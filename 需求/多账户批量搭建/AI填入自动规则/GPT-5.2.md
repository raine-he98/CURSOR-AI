## Facebook 自动规则（Notion 可读性增强表格版）

> 说明：为适配 Notion 的 Markdown 表格解析与可读性：
> - 表格单元格内不使用 HTML（如 `<br>`）
> - 将多条件拆成 `判断规则（1）/（2）/（3）` 等列
> - 条件内用 **字段名加粗** + `字段值行内代码` 标记

---

## 新手村（对象：adset）

| 规则名称 | 应用层级 | 判断时机（创建时间） | 判断规则（1） | 判断规则（2） | 判断规则（3） | 调控方式 | 调控方式细节 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 新手村-关闭adset（花费>$45, Purchase=0, CPM>$50） | `adset` | 创建 **[介于]** `0` 天 `2` 天 | **指标**：`消耗`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`累计`；**运算符**：`>`；**数值**：`45` | **指标**：`Purchase`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`累计`；**运算符**：`=`；**数值**：`0` | **指标**：`CPM`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`平均`；**运算符**：`>`；**数值**：`50` | `关停广告` | 无 |
| 新手村-关闭adset（花费>$45, Purchase=0, Checkouts Initiated=0） | `adset` | 创建 **[介于]** `0` 天 `2` 天 | **指标**：`消耗`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`累计`；**运算符**：`>`；**数值**：`45` | **指标**：`Purchase`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`累计`；**运算符**：`=`；**数值**：`0` | **指标**：`Checkouts Initiated`；**数据时间范围**：`最近2天（含当天）`；**计算方式**：`累计`；**运算符**：`=`；**数值**：`0` | `关停广告` | 无 |
| 新手村-关闭adset（2天前, Day2结束, 总ROAS≤15%） | `adset` | 创建 **[介于]** `2` 天 `N` 天（如支持精确第2天：建议 `2` 到 `2`） | **指标**：`Purchase ROAS`；**数据时间范围**：`最近N天`；**计算方式**：`累计/总计（若无则平均）`；**运算符**：`≤`；**数值**：`15` |  |  | `关停广告` | 无 |
| 新手村-调整预算至100（2天内, 当日Purchase≥2） | `adset` | 创建 **[介于]** `0` 天 `2` 天 | **指标**：`Purchase`；**数据时间范围**：`当天`；**计算方式**：`累计`；**运算符**：`≥`；**数值**：`2` |  |  | `调整广告日预算` | 目标表达：调到 `100$`；可填写：`提高 100$` 并设置 **最高调整至** `100$` |
| 新手村-调整预算至100（2天前, Day2结束, 总ROAS>15%） | `adset` | 创建 **[介于]** `2` 天 `N` 天（如支持精确第2天：建议 `2` 到 `2`） | **指标**：`Purchase ROAS`；**数据时间范围**：`最近N天`；**计算方式**：`累计/总计（若无则平均）`；**运算符**：`>`；**数值**：`15` |  |  | `调整广告日预算` | 设置 **最高调整至** `100$` |

---

## 常规（对象：adset）

| 规则名称 | 应用层级 | 判断时机 | 判断规则（1） | 判断规则（2） | 调控方式 | 调控方式细节 |
| --- | --- | --- | --- | --- | --- | --- |
| 常规-关闭adset（总ROAS≤15% 且 当日ROAS≤15%） | `adset` | 无（常规周期执行） | **指标**：`Purchase ROAS`；**数据时间范围**：`最近N天`；**计算方式**：`累计/总计（若无则平均）`；**运算符**：`≤`；**数值**：`15` | **指标**：`Purchase ROAS`；**数据时间范围**：`当天`；**计算方式**：`累计/平均（以模块支持为准）`；**运算符**：`≤`；**数值**：`15` | `关停广告` | 无 |
| 常规-预算动态调整（当日Purchase≥2） | `adset` | 无（常规周期执行） | **指标**：`Purchase`；**数据时间范围**：`当天`；**计算方式**：`累计`；**运算符**：`≥`；**数值**：`2` |  | `调整广告日预算` | 若支持“阶梯/多次触发”：每新增 `1` 个 Purchase 触发 `1` 次，单次 `+50$`；若仅支持执行一次：当日满足时（每天最多 `1` 次）`+50$` |
| 常规-复制/重启进入新手村 | `adset` | 无（系统行为） | N/A | N/A | N/A | 复制：新建 adset 天然满足新手村（`0-2` 天）；重启：若仅按创建时间，老 adset 不会进入，需支持“重启标记新手村/重置计时” |

---

## 增加规则（对象：adset）

| 规则名称 | 应用层级 | 判断时机 | 前置筛选（可选） | 判断规则（1） | 调控方式 | 调控方式细节 |
| --- | --- | --- | --- | --- | --- | --- |
| 增加-重新开启adset（2天内ROAS>20% 且 已关闭） | `adset` | 无 | **状态**：`PAUSED/关闭`（若模块支持） | **指标**：`Purchase ROAS/ROI`；**数据时间范围**：`最近3天（含当天）`；**计算方式**：`平均/累计（以模块支持为准）`；**运算符**：`>`；**数值**：`20` | `开启广告` | 无 |
| 增加-复制广告组（当天Purchase≥2） | `adset` | 无 |  | **指标**：`Purchase`；**数据时间范围**：`当天`；**计算方式**：`累计`；**运算符**：`≥`；**数值**：`2` | `复制广告` | **复制频次**：`1` 次/日；**命名**：`{复制日期}-{原广告名称}-复制-{自增序号}`；**预算**：`继承原adset/指定固定值（按模块支持）` |
